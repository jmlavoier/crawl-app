import * as React from 'react';
import * as ReactDOM from 'react-dom';

const root = document.getElementById('root');

function App() {
  return (
    <div>Init React</div>
  );
}

ReactDOM.render(
  <App />, 
  root,
);
